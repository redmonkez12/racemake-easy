# Claude Code Project Instructions

This repository uses the Ralph autonomous agent loop. Process rules live in `AGENT.md` and `PROMPT.md`.

**When skill guidance and `AGENT.md` conflict, `AGENT.md` wins.**

## Notes for This Repo

- This is a TypeScript project.
- Check `package.json` for available scripts before running validation commands.
- Follow existing TypeScript and project conventions.
